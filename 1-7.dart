import 'dart:io' show stdin, stdout ;
import 'dourt: math';

Run | Debug
void main (Listestring>args) {
stdout.write ('Enter Your Number:');
var radius, A;
radius= radius ?? stdin.readlinesyn
var radius! = int.pance (radius);
A = 3.14 * paw (radius2, 2);
print(A);
}